{
  "name": "fakeserverfactory",
  "version": "1.0.0",
  "description": "create fake server for test",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon main.js",
    "start": "npm run generate-data && node main.js",
    "generate-data": "node generate-data.js"
  },
  "keywords": [
    "json-server"
  ],
  "author": "alex tran",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.17.0",
    "query-string": "^7.1.1"
  },
  "devDependencies": {
    "@faker-js/faker": "^7.4.0",
    "faker": "^6.6.6",
    "nodemon": "^2.0.19"
  }
}
